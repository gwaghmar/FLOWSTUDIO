---
phase: 03-smarter-ai-generation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/core/src/diagram-types.ts
autonomous: true
requirements:
  - AI-01
  - AI-02
  - AI-03

must_haves:
  truths:
    - "Every diagram type in DIAGRAM_SYSTEM_PROMPTS has a SUBTYPE / LAYOUT-PATTERN / COMPOSITION-PATTERN / PROCESS-SHAPE SELECTION section"
    - "Every diagram type has a CONTENT EXTRACTION CHECKLIST"
    - "Every diagram type has at least one FEW-SHOT example"
    - "Mermaid prompt biases sequenceDiagram for prompts with ≥2 actors exchanging messages"
    - "No structural changes — DIAGRAM_SYSTEM_PROMPTS shape unchanged"
  artifacts:
    - path: "packages/core/src/diagram-types.ts"
      provides: "Type-selection rules, extraction checklists, few-shot examples for all 7 types"
      contains: "FEW-SHOT EXAMPLES"
---

<objective>
Overhaul `DIAGRAM_SYSTEM_PROMPTS` in `packages/core/src/diagram-types.ts` so every diagram type gets:
1. A type-selection / layout-pattern decision section
2. A type-specific content extraction checklist
3. 1–2 few-shot examples that demonstrate correct subtype selection (not just syntax)
</objective>

<scope>
Touch ONLY the `DIAGRAM_SYSTEM_PROMPTS` record. No exports, no consumers, no type signatures change.
</scope>

<decisions>
- D-01: Full uniform rewrite across all 7 types
- D-02: Per-type extraction checklists (mermaid actors+steps+decisions, bpmn lanes+gateways, reactflow node types+transitions, etc.)
- D-03: Few-shot examples illustrate correct *type/subtype selection*, not syntax recitation
- D-04: Changes confined to `DIAGRAM_SYSTEM_PROMPTS` record
</decisions>
