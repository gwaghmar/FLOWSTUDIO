---
phase: 03-smarter-ai-generation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/web/app/api/ai/generate/route.ts
autonomous: true
requirements:
  - AI-04

must_haves:
  truths:
    - "shouldClarify gate uses ambiguityScore >= 90"
    - "intentInstruction tells the model to set shouldAskClarification: true ONLY when a critical actor/entity/structure is missing AND no assumption can be made"
    - "Two-gate system retained — both model flag AND numeric threshold must be true"
---

<objective>
Raise the clarification gate so most prompts — even vague ones — generate immediately with noted assumptions instead of asking questions.
</objective>

<decisions>
- D-05: Server-side gate stays at `ambiguityScore >= 90` (already in place from earlier)
- D-06: Update `intentInstruction` `shouldAskClarification` rule to be conservative
- D-07: Two-gate logical AND retained
- D-08: `clampAmbiguityScore` unchanged
</decisions>
