---
phase: 03-smarter-ai-generation
plan: 03
type: execute
wave: 1
depends_on:
  - 03-02
files_modified:
  - apps/web/app/api/ai/generate/route.ts
  - apps/web/components/editor-client.tsx
autonomous: true
requirements:
  - AI-05

must_haves:
  truths:
    - "API success response includes resolvedSubtype field"
    - "Editor renders a slim banner between toolbar and canvas after silent generation"
    - "Banner content matches `Generated as: {resolvedSubtype} · {presetLabel} · {Detail} detail`"
    - "Banner auto-dismisses after 8 seconds"
    - "Banner has an × dismiss button"
    - "Banner does NOT appear when needsClarification is true"
---

<objective>
Show the user what the AI silently decided — which subtype, which preset, what detail level — without requiring them to look anywhere else.
</objective>

<decisions>
- D-09: Banner is independent from chat assistantMessage; both coexist
- D-10: Format `Generated as: {resolvedType} · {presetLabel} · {Detail} detail`
- D-11: Backend adds `resolvedSubtype` (from `intentPlan.suggestedSubtype ?? effectiveDiagramType`)
- D-12: 8-second auto-dismiss + manual × button
- D-13: Only when `needsClarification === false` AND `resolvedSubtype` is present
- D-14: Slim 36px bar, neutral muted style
</decisions>
