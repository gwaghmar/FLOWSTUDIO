---
phase: 01-wysiwyg-canvas
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified: []
autonomous: false
requirements:
  - WYSIWYG-01
  - WYSIWYG-02
  - WYSIWYG-03
  - WYSIWYG-04

must_haves:
  truths:
    - "All 5 social presets render the canvas at their correct aspect ratio"
    - "Custom preset renders canvas at 16:9 on fresh load"
    - "Dimension label is visible in the toolbar without opening Export menu"
    - "Switching presets immediately updates the canvas shape without page reload"
  artifacts:
    - path: "apps/web/components/editor-client.tsx"
      provides: "Verified WYSIWYG canvas implementation"
  key_links: []
---

<objective>
Human verification of WYSIWYG canvas behavior across all 5 social presets and custom dimensions.

Purpose: Confirm visually that the canvas container renders at the correct aspect ratio for each preset — what you see is what you export.

Output: Confirmed pass or bug report for targeted fixes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-wysiwyg-canvas/01-01-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify WYSIWYG canvas across all presets</name>
  <action>Human visual verification of WYSIWYG canvas behavior built in Plan 01. Follow the steps in how-to-verify below.</action>
  <what-built>
    - Preset selector in canvas toolbar (no need to open Export menu)
    - Dimension label (e.g. "1920×1080") always visible in toolbar
    - Mermaid canvas container enforces aspect-ratio via CSS
    - Custom preset defaults to 1920×1080 (16:9)
  </what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm dev` (runs on http://localhost:3040)
    2. Open the editor (create or open any diagram)
    3. Select the **Mermaid** diagram type

    **Preset: Landscape (16:9)**
    4. In the canvas toolbar, select "Landscape (16:9)" from the preset dropdown
    5. ✓ Canvas container is visibly wider than tall (roughly 16:9 shape)
    6. ✓ Toolbar shows "1920×1080" dimension label

    **Preset: Story / Reel (9:16)**
    7. Select "Story / Reel (9:16)" from the toolbar preset dropdown
    8. ✓ Canvas container is visibly taller than wide (roughly 9:16 shape)
    9. ✓ Toolbar shows "1080×1920"

    **Preset: Square feed (1:1)**
    10. Select "Square feed (1:1)"
    11. ✓ Canvas container appears as a perfect square
    12. ✓ Toolbar shows "1080×1080"

    **Preset: Custom (default 16:9)**
    13. Clear localStorage (DevTools → Application → Local Storage → delete "flowchart-export:customWidth" and "flowchart-export:customHeight") and hard refresh
    14. Select "Custom" from the toolbar dropdown
    15. ✓ Canvas displays at 1920×1080 (16:9 shape) by default
    16. ✓ Toolbar shows "1920×1080"

    **Immediate update check**
    17. Switch rapidly between presets
    18. ✓ Canvas shape updates immediately on each selection (no page reload needed)
  </how-to-verify>
  <verify>Human visual confirmation via the steps above</verify>
  <done>All 5 presets show correct aspect ratio; dimension label visible in toolbar; custom defaults to 16:9; preset switching is instant</done>
  <resume-signal>Type "approved" if all checks pass, or describe which checks failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | Verification-only plan — no code changes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | N/A | Verification plan | accept | No new code — all threats addressed in Plan 01 threat model |
</threat_model>

<verification>
Human sign-off per Task 1 checklist above.
</verification>

<success_criteria>
- All 5 presets render canvas at correct aspect ratio
- Dimension label visible in toolbar (not just Export flyout)
- Custom defaults to 1920×1080
- Switching presets is instantaneous
</success_criteria>

<output>
After completion, create `.planning/phases/01-wysiwyg-canvas/01-02-SUMMARY.md`
</output>
