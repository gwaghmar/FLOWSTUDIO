# 2026-04-09

- User wants Chrome-on-PC (DevTools MCP) as the default for UI/UX testing; recorded in `MEMORY.md`.
- Homepage UX: logo links home, primary CTA passes `callbackUrl=/app/editor`, helper copy under CTAs, skip link + `main` landmark, focus-visible rings on interactive elements.
- Pricing/docs: aligned background and minimum height with marketing pages.
- Chrome DevTools click-through at **1280×800** and **390×844**: home nav, CTAs, pricing/docs/login/privacy flows OK. **Skip to content** is keyboard-first (`sr-only`); pointer automation cannot click it (expected).
- Privacy + Terms: added **← Home**, `dot-grid-bg`, focus ring on home link (was a dead end).
