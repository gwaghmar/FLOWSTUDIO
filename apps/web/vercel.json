{
  "installCommand": "cd ../.. && pnpm install --frozen-lockfile",
  "buildCommand": "cd ../.. && pnpm --filter @flowchart/core build && pnpm --filter @flowchart/web build",
  "crons": [
    { "path": "/api/health", "schedule": "0 12 * * *" }
  ]
}
